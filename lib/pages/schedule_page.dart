import 'package:volo_meeting/index.dart';

class SchedulePage extends StatelessWidget {
  const SchedulePage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('预订会议'),
      ),
    );
  }
}
